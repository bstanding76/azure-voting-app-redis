trigger:
  - master

pool:
  vmImage: 'ubuntu-latest'

stages:
- stage: Build
  displayName: 'Build and push docker images Demo'
  variables:
    - template: vars/vars-global.yaml
  jobs:
  - template: ${{ variables.ciBuildPath }} # Template reference
